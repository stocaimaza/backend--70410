{{#each imagenes}}
    <div>
        <img src={{this.path}} alt={{this.title}}>
        <h3> {{this.title}} </h3>
        <p> {{this.description}} </p>
        <a href=""> Eliminar </a>
    </div>
{{/each}}